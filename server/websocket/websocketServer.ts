// Vercel-compatible stub version
// For full implementation with ws (WebSocket), see websocketServer.local.ts

/**
 * WebSocket Server Stub for Vercel Deployment
 *
 * This is a placeholder that allows the documentation site to build on Vercel.
 * For local development with full WebSocket functionality, use package.local.json
 * which includes ws dependency.
 *
 * Production deployment requires:
 * - Pusher, Ably, or similar managed WebSocket service
 * - Or polling with API routes
 * - SSE (Server-Sent Events) for one-way updates
 */

console.warn('WebSocket server stub loaded - WebSocket functionality not available on Vercel');
console.warn('For real-time updates on Vercel, use Pusher, Ably, or polling');

// Export empty to satisfy any imports
export {};
